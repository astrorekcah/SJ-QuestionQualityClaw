{
  "path": "secure-coding/identification-authentication-failures/improper-authentication/sc-owp-iaf-Python-2",
  "title": "(Python) Identification and Authentication Failures | Weak Password Recovery Mechanism",
  "parameters": {
    "programmingLanguage": [
      "python"
    ]
  },
  "prompt": {
    "typeId": "mc-line",
    "configuration": {
      "prompt": "While reviewing a financial services platform's password recovery system, you discover code handling customer account resets. The system processes thousands of password resets daily across multiple regions. Which line introduces a critical authentication vulnerability that could allow unauthorized access?",
      "code": [
        "import logging",
        "import secrets",
        "import smtplib",
        "from email.mime.text import MIMEText",
        "from datetime import datetime",
        "from typing import",
        "from flask import Flask, request, jsonify",
        "",
        "app = Flask(__name__)",
        "logger = logging.getLogger(__name__)",
        "",
        "",
        "class PasswordRecovery:",
        "    def __init__(self):",
        "        self.users = {}",
        "        self.reset_tokens = {}",
        "",
        "    def generate_reset_token(self) -> str:",
        "        return ''.join(secrets.choice('0123456789') for _ in range(4))",
        "",
        "    def clear_expired_tokens(self):",
        "        now = datetime.utcnow()",
        "        expired = [",
        "            email for email, data in self.reset_tokens.items()",
        "            if (now - data['created']).seconds > 3600",
        "        ]",
        "        for email in expired:",
        "            del self.reset_tokens[email]",
        "",
        "    def send_reset_email(self, email: str, token: str):",
        "        try:",
        "            msg = MIMEText(f'Your password reset code is: {token}')",
        "            msg['Subject'] = 'Password Reset Request'",
        "            msg['From'] = 'security@company.com'",
        "            msg['To'] = email",
        "",
        "            with smtplib.SMTP('smtp.company.com') as server:",
        "                server.starttls()",
        "                server.login('smtp_user', 'smtp_pass')",
        "                server.send_message(msg)",
        "",
        "            logger.info('Reset email sent', extra={'email': email})",
        "",
        "        except Exception as e:",
        "            logger.error('Failed to send email', exc_info=True)",
        "            raise",
        "",
        "    def request_reset(self, email: str) -> bool:",
        "        if email not in self.users:",
        "            logger.warning('If an account with that email exists, we have sent password reset instructions to the registered address.',",
        "                           extra={'email': email})",
        "            return False",
        "",
        "        self.clear_expired_tokens()",
        "        token = self.generate_reset_token()",
        "",
        "        self.reset_tokens[email] = {",
        "            'token': token,",
        "            'attempts': 0,",
        "            'created': datetime.utcnow()",
        "        }",
        "",
        "        self.send_reset_email(email, token)",
        "        return True",
        "",
        "    def verify_reset(self, email: str, token: str, new_password: str) -> bool:",
        "        if email not in self.reset_tokens:",
        "            return False",
        "",
        "        token_data = self.reset_tokens[email]",
        "        token_data['attempts'] += 1",
        "",
        "        if token_data['attempts'] >= 3:",
        "            del self.reset_tokens[email]",
        "            logger.warning('Too many reset attempts',",
        "                           extra={'email': email})",
        "            return False",
        "",
        "        if (datetime.utcnow() - token_data['created']).seconds > 3600:",
        "            del self.reset_tokens[email]",
        "            return False",
        "",
        "        if token == token_data['token']:",
        "            self.users[email]['password'] = new_password",
        "            del self.reset_tokens[email]",
        "            logger.info('Password reset successful',",
        "                        extra={'email': email})",
        "            return True",
        "",
        "        return False",
        "",
        "",
        "recovery = PasswordRecovery()",
        "",
        "",
        "@app.route('/api/v1/password/reset', methods=['POST'])",
        "def request_reset():",
        "    email = request.form.get('email')",
        "    if not email:",
        "        return jsonify({'error': 'Email required'}), 400",
        "",
        "    if recovery.request_reset(email):",
        "        return jsonify({'status': 'Reset email sent'})",
        "    return jsonify({'error': 'Invalid email'}), 404",
        ""
      ],
      "choices": [
        {
          "key": "a",
          "choice": 19
        },
        {
          "key": "b",
          "choice": 33
        },
        {
          "key": "c",
          "choice": 49
        },
        {
          "key": "d",
          "choice": 73
        }
      ]
    }
  },
  "answers": [
    {
      "value": "a"
    }
  ]
}